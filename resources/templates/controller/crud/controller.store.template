$validate = request()->validate([
            {{validateFields}}
        ]);

        ${{modelInstance}} = {{modelInstance}}::create($request->all());

        return [ 'msj' => 'Registro Agregado Correctamente', compact('{{modelInstance}}') ];