 
        <v-flex xs12 sm6>
            <v-text-field
                :rules="rules.required"
                v-model="form.{{columnName}}"
                label="{{labelName}}"
                placeholder="Indique {{labelName}}"
            ></v-text-field>
        </v-flex>
                 