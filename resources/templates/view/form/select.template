         
        <v-flex xs12 sm6>
            <v-select
            :items="lists.{{fkTableName}}"
            item-text="{{fkColTableName}}"
            item-value="{{columnName}}"
            v-model="form.{{columnName}}"
            :rules="rules.select"
            label="{{labelName}}"
            autocomplete
            required
            ></v-select>
        </v-flex>
         